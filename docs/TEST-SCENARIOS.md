# TEST-SCENARIOS.md — the test-scenario registry

A single, living registry of every load-bearing test scenario in cosmos, across all
test layers. **Owned by the test agent** (`test-engineer`): the sdd/bugfix cycles delegate
test authoring to it, and it MUST read + update this file before writing new tests.

## Why this exists

Tests kept contradicting each other and silently regressing. The classic case: a
"channel name above the list" test wanted a header stacked above a message list, while a
"per-list independent scroll" test required multiple lists side-by-side — both controlled
by the SAME CSS seam, so a fix for one broke the other (and node unit tests, which only
assert class strings, could not see it). A central registry lets the test agent detect
that a NEW scenario would contradict an EXISTING invariant **before** writing the test,
instead of discovering it as a red→whack-a-mole later.

## How the test agent uses it

1. **Before writing tests:** scan this file for scenarios touching the same module / CSS
   seam / IPC channel / behavior. If the new test would contradict an existing invariant,
   STOP and surface the conflict (it is a product decision, not a test detail).
2. **After writing tests:** add/lift each new scenario here (id, invariant, layer, file),
   and record any cross-scenario tension in **Known tensions** below.
3. **When a fix changes an invariant:** update the affected row(s) here in the same change,
   so the registry never lies about what is asserted.

## Test layers (necessary AND sufficient bar)

| Layer | Env / runner | Catches | Script |
|-------|--------------|---------|--------|
| node-unit | vitest, node, no jsdom | pure logic, class-string construction, reducers | `npm test` |
| node-integration | vitest, node, real fs temp dirs + injected spawn/clock | IPC handlers, the `cosmos-file` protocol stream, agentRunner spawn/queue, MCP stdio | `npm run test:integration` |
| jsdom-component | vitest, jsdom + testing-library | component render, DOM events, hooks (scroll, keymap, focus) | `npm run test:dom` |
| visual | Playwright + Vite test page | computed layout / pixels (side-by-side, fill, scroll position, canvas) | `npm run test:visual` |
| e2e | Playwright `_electron` (real app) | preload bridge, custom protocol, session lifecycle end-to-end | `npm run test:e2e` |

> **Bar for "done":** a renderer/UI change needs a jsdom or visual test; a main-process
> server / IPC / protocol change needs a node-integration (or e2e) test. Green `npm test`
> (node-unit) + `typecheck` are NECESSARY but NOT SUFFICIENT — they cannot see runtime.

## Scenario registry

| ID | Invariant (what must hold) | Layer | Test file |
|----|----------------------------|-------|-----------|
| SL-SCROLL-01 | Two+ Slack message lists lay out side-by-side (same top, different left), each its own scroll container, independent scrollTop, filling panel height (no unified outer scroll) | visual | `tests/visual/layout.visual.spec.ts` |
| SL-SCROLL-02 | A non-list header (channel name) in a Column stacks ABOVE its list (header.bottom ≤ list.top) — withOUT breaking SL-SCROLL-01 | visual | `tests/visual/layout.visual.spec.ts` |
| SL-SCROLL-03 | A history list opens scrolled to the LATEST (bottom) message on initial load / channel switch; a top load-more (prepend) preserves position. Applies to BOTH the native (`kind='radix-viewport'`) and the GENERATIVE catalog (`kind='self'`) list — `useSlackScrollToLatest` holds its target in STATE (a `{current}`-shaped handle that re-keys the layout effect on attach) so the self-mode bottom-jump actually fires (bug slack-generative-scroll-to-latest-v1; was silently skipping). | visual + node-unit + jsdom | `tests/visual/layout.visual.spec.ts`, `src/renderer/slackScrollToLatest.test.ts`, `src/renderer/useSlackScrollToLatest.dom.test.tsx` (jsdom self-mode wiring — green) |
| SL-PAGE-01 | Scrolling near the top auto-loads the older page once (guarded), and the prepend preserves scroll position (anchor) | jsdom + node-unit | `src/renderer/slackScrollPaginate.test.ts`, `src/renderer/useSlackScrollPaginate.dom.test.tsx` (jsdom hook wiring — green) |
| SL-PAGE-02 | The GENERATIVE catalog `MessageList` also auto-loads older messages on scroll-to-top: `useSlackScrollPaginate` accepts `kind='self'` (the `SLACK_LIST_SCROLL_CLASS` div IS the scroller; no Radix viewport descendant) and, on a near-top scroll with a next page (`hasMore`) and not-loading (`loading`), fires the SAME `adapter.loadMore` action `LoadMoreButton` dispatches (`{ surfaceId, region? }`) — renderer-side only, no new adapter contract (the older page accumulates main-side via `updateDataModel`). Guarded against double-fire; exhausted (`hasMore=false`) never fires. | jsdom + node-unit | `src/renderer/slackScrollPaginate.test.ts`, `src/renderer/useSlackScrollPaginate.dom.test.tsx` (`kind='self'` cases — green) |
| FV-PDF-01 | A document (pdf/docx/xlsx) opened from a tab root loads its bytes over the typed `window.cosmos.fs.readBytes` IPC — NOT a cross-scheme `cosmos-file` fetch/XHR (Chromium refuses a custom scheme from the http dev origin). e2e asserts the bridge exists + returns a typed result (never throws); the http visual harness still asserts the react-pdf canvas renders. Full canvas-render through a LIVE pane root remains a manual check (e2e has no programmatic `claude` cwd). | e2e (real IPC bridge) + visual (http canvas) + node-integration (bytes) | `tests/e2e/app.e2e.spec.ts` (readBytes bridge), `tests/visual/layout.visual.spec.ts` (canvas), `src/main/fsExplorer.integration.test.ts` (readBytes bytes) |
| FS-PROTO-01 | Both byte paths confine + size-cap: the `cosmos-file` protocol handler (`<img>` images) streams an in-root file (200) / refuses out-of-root/forged/missing/dir (non-2xx); the `fs:readBytes` IPC (pdf/docx/sheet documents) returns `{ok:true,bytes}` for in-root, `too-large` over the per-format cap, and `out-of-root`/`not-found` for forged/escaped/missing — never throws, no absolute path leaks | node-integration | `src/main/localFileProtocol.integration.test.ts` (img scheme), `src/main/fsExplorer.integration.test.ts` (readBytes IPC) |
| AGENT-SESS-01 | Serialized same-session runs NEVER collide on the session id ("already in use"); every target passes the persistent `--session-id`; submits queue + drain one at a time. THE LOAD-BEARING PART (session-id-already-in-use-runtime-v1): the serializer alone is NOT enough — the just-exited `claude` child can still hold its `~/.claude/sessions/<pid>.json` REGISTRY entry when the next same-id run is drained, so claude rejects it "Session ID … is already in use". The runner now mirrors the PTY `--resume` path: on an in-use exit it plans a backoff via the shared `planResumeRetry` (injected `SessionLockEnv`) and RE-SPAWNS the SAME submit after the delay instead of erroring + draining. Integration test injects a stale dead-pid registry stub, fakes timers, and asserts the queued child is NOT spawned immediately and IS spawned after the backoff (RED without the retry wiring, GREEN with it); also covers retry-then-drain success and budget-exhausted give-up. | node-integration (green) | `src/main/agentRunner.integration.test.ts` |
| TAB-NAV-FOCUS-01 | Cmd+Opt+Arrow (`tab:next`/`tab:prev`) moves the FILE tabs when the editor/viewer pane holds focus, and the TERMINAL tabs otherwise — the routing follows focus, not a fixed strip (terminal-focus-aware-tab-nav-v1; was always moving terminal tabs). Pure routing predicate + cycle stay in `.ts`; the focus→routing wiring is asserted in jsdom | node-unit + jsdom | `src/renderer/closeTabRouting.test.ts`, `src/renderer/panelTabs.test.ts` (cycle), `src/renderer/TerminalTabNavRouting.dom.test.tsx` |
| TAB-NAV-FOCUS-02 | When the REAL Monaco editor holds focus, `viewerFocused` flips true so Cmd+Opt+Arrow moves the FILE tabs (terminal-tab-nav-monaco-focus-v1; was moving terminal tabs). GOTCHA — Monaco creates its editor with NO `overflowWidgetsDomNode`, so it mounts its hidden keyboard-input `<textarea>` on `document.body`, OUTSIDE the FileViewer subtree; the editor's DOM `focusin` therefore NEVER bubbles to the FileViewer outer div's `onFocus`. The fix drives viewer-focus from Monaco's OWN `onDidFocusEditorText`/`onDidBlurEditorText` in `MonacoText`, not DOM bubbling. TEST REQUIREMENT — a plain-div `fireEvent.focus` test (the original TAB-NAV-FOCUS-01 harness) is structurally insufficient and gives false confidence: it CANNOT reproduce Monaco mounting its input outside the tree. `MonacoFocusNav.dom.test.tsx` asserts the REAL `MonacoText`→`onViewerFocusChange` path (RED on the un-wired editor: no listeners registered → undefined; GREEN with the fix), mocking only the `monaco` namespace because monaco-editor cannot construct under jsdom (needs DOMMatrix/canvas/layout). The FULL real-editor end-to-end (a real textarea on body + a real keystroke moving the file tab) is ONLY catchable by e2e against a live pane — deferred: `COSMOS_E2E` mode spins up no programmatic `claude` cwd, so opening a real file in the editor through e2e is not yet wired (same gap as FV-PDF-01's live-pane canvas). | jsdom (real MonacoText wiring) + node-unit (routing) | `src/renderer/fileExplorer/MonacoFocusNav.dom.test.tsx`, `src/renderer/closeTabRouting.test.ts` |
| VIEWER-FOCUS-NONMONACO-01 | A dedicated (non-editor) viewer body is focusable so the focus-aware file-tab shortcuts route (viewer-keyboard-shortcut-nonmonaco-focus-v1). ROOT CAUSE — the focus-aware `Ctrl/Cmd+W` (close file tab) + `Cmd+Opt+Arrow` (file-tab nav) routing keys off the viewer's focus-within (`onViewerFocusChange`), which fires from the body wrapper's `onFocus`/`onBlur` (focusin/focusout bubbling). Monaco's hidden `<textarea>` is focusable so an editor click establishes focus-within (TAB-NAV-FOCUS-02), but the DEDICATED viewers (`PdfView`/`DocxView`/`SheetView`/inline image/state blocks) have ZERO focusable children, so a click on a PDF/image never moved DOM focus into the subtree → `onViewerFocusChange(true)` never fired → the shortcuts were dead for those tabs. FIX — the populated body wrapper itself becomes the focus target (`tabIndex={-1}`, keeping `outline-none`) and is `.focus()`-ed when a NON-editor file becomes active (and on a body mousedown that didn't land on a focusable child); the editor (`text`) case is skipped so Monaco keeps owning its own textarea focus (no focus-steal). The jsdom test renders the REAL `FileViewer` with a `pdf` ViewerState + ≥1 open file + an `onViewerFocusChange` spy and asserts the activate focuses the wrapper (`document.activeElement` carries `tabindex=-1`) AND the spy is called `true` (RED without the fix: no focusable target → focus-within never true; GREEN with it). The Monaco case (TAB-NAV-FOCUS-02) in the same file must stay GREEN — the editor must still drive its own focus, not have it stolen. | jsdom (real FileViewer focus wiring) | `src/renderer/fileExplorer/MonacoFocusNav.dom.test.tsx` |
| CF-COMMENT-AUTHOR-01 | A footer-comment's author resolves to the display NAME (not the raw account id) on top-level comments AND replies: `getComments` reads the realistic v2 author (`version.authorId`), resolves it via the SAME authed `GET /wiki/rest/api/user` path (Bearer attached), and reads the v1 user body's top-level `displayName`. The runtime fix is the granular `read:user:confluence` OAuth scope — WITHOUT it the user endpoint 403s and the author degrades to the raw id (node-unit stubs masked this; the integration test models the gateway 403 so it is RED on the missing scope). A user-lookup failure degrades to the raw id (never throws) | node-integration | `src/main/confluenceComments.integration.test.ts` |
| CMP-MODE-01 | The single shared Open-Prompt composer has a per-surface MODE (cosmos-open-prompt-pinned-v1): Cosmos ⇒ `docked` (always-open, bottom-pinned INSET rounded card; never collapses on submit/Esc/click-outside; NOT hidden by `busy`; auto-focus on the Cosmos activation edge without focus-steal; submit stays open + clears draft), every OTHER surface (slack/jira/confluence/google-calendar/terminal) ⇒ `floating` (the unchanged draggable collapse-on-exit logo). `composerModeForSurface` is the SINGLE place the rule lives (invalid input ⇒ `floating`, no throw); the `.tsx` reads only the pure predicates `isAlwaysOpen`/`allowsCollapse`/`hidesOnBusy`. NODE rows are the FR-011 regression guard for the four floating panels (their predicates must stay collapse-permitted + hide-on-busy). The jsdom rows render the REAL `PromptComposer` in `mode="docked"` because the rendered DOM behavior (always-present textarea, inert Esc, stay-open submit, not-hidden-on-busy, activation-edge auto-focus) is NOT visible to node-unit predicates. REGRESSION (cosmos-panel-tab-list-v1): the docked render branch previously OMITTED the `ContextChip` entirely (Cosmos had no view context), so a tree-click panel+tab selection had no visible affordance — the docked branch now renders the SAME chip the floating composer does; jsdom rows assert a `kind:'panel-tab'` `contextChip` renders a `role="note"` breadcrumb (panel+tab labels) with a removable `×` that hides it (`contextDismiss:'all'`). | node-unit + jsdom | `src/renderer/activeComposer.test.ts` (mode mapping), `src/renderer/promptComposerLogic.test.ts` (mode predicates), `src/renderer/PromptComposerDocked.dom.test.tsx` (docked DOM behavior + docked context chip) |
| CMP-OPENPOS-01 | Clicking the floating Open-Prompt logo ALWAYS opens the composer CARD (open-prompt-card-never-opens-v1) and CENTERED over the panel, NEVER at the top-left (≈0,0) anchor (open-prompt-opens-top-left-v1). Both come from ONE pure source of truth, `resolveCardPlacement`, which encodes TWO independent thresholds (conflating them caused the paired regressions): `show` = the card may be shown AT ALL, gated ONLY on the PANEL being measured (`panel.width>0`) — RELIABLE, and crucially NOT on the card's own measurement (the card's `w-full` width derives from the panel-sized layer, so it measures `{0,0}` until the panel is sized and may never re-measure non-zero; gating show on it trapped the card invisible → "대화창이 안 뜸"); `anchored` = the precise button-anchored `cardPx` is usable, gated on the CARD measuring a real non-zero box. Render states: NOT shown ⇒ `invisible` + centered fallback (hide-until-measured); shown-but-NOT-anchored ⇒ VISIBLE + centered over the panel via dimension-independent CSS (`left-1/2 top-1/2 -translate-1/2`, no card dims) so the card opens even if its own measurement never lands, centered not top-left; anchored ⇒ `left-0 top-0` + inline `translate3d(cardPx)`. The card-measure effect also re-runs on `panelRect` change so a panel-sized form re-measures to `anchored` without relying on the ResizeObserver. WHY A JSDOM ROW IS REQUIRED + WHY MOCKING-THE-MEASURED-STATE IS INSUFFICIENT: the pure `openPromptPosition.test.ts` only clamps WITH a measured card; and a jsdom test that mocks BOTH `getBoundingClientRect` AND the card `offsetWidth/Height` only proves "given a measured card, it shows" — it never exercises the real open→measure transition and stayed GREEN while the card never opened (the exact gap that let open-prompt-card-never-opens-v1 through). The strengthened jsdom test renders the REAL `PromptComposer` floating and drives the open with progressively more layout mocked: PANEL UNMEASURED (no mock) ⇒ hidden + centered; PANEL measured but CARD `offsetWidth` left at 0 (real jsdom) ⇒ card SHOWN + centered, not invisible (RED on the over-gated wiring — the "never opens" guard); PANEL+CARD measured ⇒ shown, button-anchored at x>0 AND y>0 (not top-left). | node-unit + jsdom | `src/renderer/composer/openPromptPosition.test.ts` (`resolveCardPlacement` show/anchored thresholds), `src/renderer/composer/PromptComposerFloatingOpenPosition.dom.test.tsx` (real render open→measure transition) |
| AT-OAUTH-RECONSENT-01 | The Atlassian authorize URL FORCES re-consent (`prompt=consent`) on every connect/reconnect, so a reconnect always mints a token with the CURRENT full scope set instead of Atlassian silently reusing the prior consent + OLD scopes (the cause of CF-COMMENT-AUTHOR-01 surviving a reconnect: the new `read:user:confluence` scope was never granted, the authed-user lookup 403'd, and the author fell back to the raw account id). The built URL carries `prompt=consent` plus all required Atlassian params (`audience`, `client_id`, `response_type=code`, `state`, loopback `redirect_uri`), the PKCE `code_challenge`+`code_challenge_method=S256` (refresh/PKCE redirect intact, `offline_access` present), and the full Confluence scope set incl. `read:user:confluence`; the `client_secret` NEVER appears in the URL (SC-009). Shared by Confluence + Jira. NOTE: this only forces the consent SCREEN — a user who already granted scopes still reconnects ONCE after this fix to pick up the user-read scope (Atlassian honors `prompt=consent` only on the next authorize). | node-unit | `src/main/integrations/atlassianOAuth.test.ts` |
| PROMPT-CTX-MARKER-01 | The `<cosmos:context>` marker codec round-trips a PromptContext (`panel` always; `tab`/`dock` omitted when absent; dock reuses the LITERAL ViewContext item fields, no fabricated label) through the pinned trailing `\n\n<cosmos:context>{json}</cosmos:context>` block (`MARKER_RE = /\n*<cosmos:context>[\s\S]*?<\/cosmos:context>\s*$/`). Collision-safety: an ordinary-prose corpus (multi-line prompts mentioning panels/tabs/brackets/`<context>`/`cosmos:context`) parses as NO-marker, prose intact. Defensive degrade (FR-010/FR-020/FR-025): a missing/wrong-shape/oversized (~4 KB cap) ctx serializes to `''` (submit never blocked); a bad-JSON / partial-field / unknown-id / dangling-tag marker drops to NO context AND still strips the trailing tag so the raw marker is never shown. Pure node module — browser-safe (no `Buffer`). | node-unit | `src/shared/promptContext/promptContextMarker.test.ts` |
| PROMPT-CTX-CHANNELS-01 | `buildAgentSubmitWithMarker` is the ONE-source-two-channels chokepoint: a ctx with a dock yields BOTH the trailing marker (channel b) AND `viewContext` = the dock's literal ViewContext fields minus `kind` (channel a, byte-identical to today's grounding); a no-dock ctx → marker present, `viewContext` absent. THE KEY GUARANTEE (SC-010/SC-011): an oversized/dropped marker (serialize → `''`) leaves the utterance plain BUT `viewContext` is STILL derived from `ctx.dock` — a marker failure can never weaken grounding. | node-unit | `src/shared/promptContext/buildAgentSubmit.test.ts` |
| PROMPT-CTX-PARSE-01 | `parseTranscript` inspects EACH user-prompt turn (both the string-content and text-block branches) for the marker → attaches `UserPromptTurn.context` + strips the marker from the displayed `text`; an absent/malformed marker → plain text, no context (pre-feature history safe); a marker-only turn (no prose) renders no empty bubble; a marker the model ECHOED in an assistant turn is stripped (no context attached), so the raw marker is never surfaced in ANY turn (FR-025). Co-exists with the existing turn-mapping assertions (no-marker turns keep their exact `{kind,id,ts,text}` shape — `context` only added when present). | node-unit | `src/main/fs/transcriptParse.test.ts` |
| PROMPT-CTX-CLAUDEMD-01 | The embedded agent's sandbox-cwd CLAUDE.md (`SANDBOX_CLAUDE_MD`) documents the trailing `<cosmos:context>` block as the user's SCREEN context (panel/tab + open dock), tells the engine to build Generated-UI that applies to it (esp. the dock item, e.g. PROJ-123), frames it as context to READ not echo/leak, references the pinned non-secret fields and NO secret field/value. `provisionSandboxClaudeMd(dir, fs)` writes `<dir>/CLAUDE.md` via injected fs and NEVER throws on a write failure (warns, best-effort — startup never blocks). | node-unit | `src/main/agent/sandboxClaudeMd.test.ts` |
| PROMPT-CTX-CHIP-01 | The read-only timeline `PromptContextChip` renders the panel dimension always, tab only when present, dock only when present (all four kinds + the slack `Thread` sub-segment), reusing the composer `contextChipFor`/`PRIMARY_ICON` ↳ treatment for the dock (SC-009) and `SURFACE_ICON`/`AppWindow` for panel/tab; `undefined` context → renders nothing (FR-021); the raw `<cosmos:context>` syntax never reaches the surface. The live-generating entry carries `promptContext` through `reconcileTimeline` so the breadcrumb is stable live→confirm (FR-024, asserted node-side in `cosmosConversation.test.ts`). PLACEMENT SUPERSEDED by cosmos-context-message-combined-box-v1 (D-11): the breadcrumb is now the static IN-BUBBLE HEADER of the combined `UserMessageBox` (header → divider → body in ONE `bg-primary` box — see COSMOS-COMBINED-BOX-01), NOT a free-standing pill above it; the header still precedes the body in DOM order (`note.compareDocumentPosition(body) & FOLLOWING`). It renders on a HISTORICAL `user-prompt` turn whose parsed `context` is attached by `transcriptParse` — not only on the live turn (cosmos-context-chip-position-and-historical-v1 #1/#2); both asserted by rendering the real `CosmosTimelineEntry` (historical-with-context → header+divider+body in one box; historical-no-context → plain bubble, no `role="note"`, no divider). The shared `PromptContextBreadcrumb` is tone-neutral (labels `currentColor`, glyphs `size-3 opacity-70`) and the standalone `PromptContextChip` is a thin `Badge variant="secondary"` shell over it (its dom permutation tests assert content/ARIA, not glyph hex/size, so the extraction keeps them green). REAL ROUND-TRIP GUARD (cosmos-context-chip-crosspanel-and-historical-v1 #1): the prior historical row INJECTED `turn.context`, proving render but NOT the codec — so a faithful guard drives the WHOLE chain with the REAL codec for a NON-cosmos panel (`serializePromptContextMarker(jiraCtx)` → append to the utterance → `parsePromptContextMarker` (the exact read-side codec `parseTranscript` delegates to) → build the `user-prompt` turn from the parser's `{context,text}` output, never a hand-built context → render) and asserts the chip shows THAT panel (Jira issue PROJ-123 / Slack #general), the bubble shows the stripped clean prose, and the raw marker never reaches the DOM. The jsonl-line extraction around the codec stays covered node-side (PROMPT-CTX-PARSE-01); the `.ts`/`.test.ts` project split keeps the renderer dom test off the main-side parser, so it drives the shared codec the parser delegates to. | jsdom + node-unit | `src/renderer/cosmos/PromptContextChip.dom.test.tsx`, `src/renderer/cosmos/cosmosConversation.test.ts`, `src/main/fs/transcriptParse.test.ts` |
| COSMOS-XPANEL-CTX-01 | The Cosmos timeline's LIVE in-flight chip reflects the ACTUAL submitting panel, not a cosmos-only default (cosmos-context-chip-crosspanel-and-historical-v1 #2). THE BUG: a submit from Jira/Slack/Confluence/Calendar goes through `useGenerativePanelTabs.submit` (which captures the correct `PromptContext` — panel/tab/dock), but the cosmos `agent:status 'started'` live seed read a ref written ONLY by `CosmosPanel.onSubmit` (hard-codes `panel:{id:'cosmos'}`), so the cross-panel context never reached the seed → the live chip always showed "Cosmos". THE FIX (renderer-only, no contract/IPC change): a SHARED "last submitted PromptContext" ref on the App-root `ActiveComposerProvider` (`useRecordSubmitContext` writes, `useLastSubmitContextRef` reads); BOTH submit sites write it at SEND time, the cosmos `'started'` seed reads it. WHY A JSDOM ROW: the seed→reconcile→render chain is only exercised end-to-end at the DOM level; the test renders the REAL `CosmosPanel` under `ActiveComposerProvider`, has a sibling consumer call the SAME `useRecordSubmitContext()` that `useGenerativePanelTabs.submit` calls (recording a JIRA/Slack context — the cross-panel path, NOT `CosmosPanel.onSubmit`), fires `agent:status 'started'`, and asserts the live chip names Jira (PROJ-123) / Slack (#general) and never "Cosmos". RED→GREEN: before the fix the seed read the cosmos-only ref so no Jira chip rendered (test red); after, it shows the submitting panel. | jsdom | `src/renderer/cosmos/CosmosCrossPanelLiveContext.dom.test.tsx` |
| COSMOS-COMBINED-BOX-01 | The Cosmos user-prompt turn renders the submit-time context + the message as ONE combined `UserMessageBox` (cosmos-context-message-combined-box-v1, FR-001..FR-011 / D-11/D-18), replacing the prior chip-ABOVE-bubble stack. Context PRESENT → ONE right-aligned `bg-primary text-primary-foreground max-w-chat-bubble rounded-2xl overflow-hidden` box holding, top→bottom: a STATIC context HEADER (`PromptContextBreadcrumb` in a `text-primary-foreground/80` container) → a `border-t border-primary-foreground/20` DIVIDER (on the body section) → the ALWAYS-visible message BODY (`whitespace-pre-wrap break-words`, today's escaped text). The header is STATIC — NO toggle, NO `aria-expanded`, NO button, body always in the DOM (explicitly the OPPOSITE of the collapsible ToolCallRow — FR-005/FR-006); the header (`role="note"`) precedes the body in DOM order WITHIN the one box. Context ABSENT/null → the box renders the body ONLY — no header, no divider (no `border-t`), no `role="note"` — visually identical to today's plain bubble (FR-009/SC-004); the divider never appears alone. LIVE (`live-generating`) + HISTORICAL (`user-prompt`) turns render the IDENTICAL box chrome (asserted byte-equal `className`), so the box is stable across the live→confirmed transition + the assistant `TypingIndicator` still renders below it (FR-010/SC-005). A long body keeps `max-w-chat-bubble` + `break-words`/`whitespace-pre-wrap` (SC-006). Renderer-only — NO IPC/contract/marker change. | jsdom | `src/renderer/cosmos/PromptContextChip.dom.test.tsx` |
| CONV-VALIDATE-CTX-01 | The OUTBOUND conversation IPC boundary validator PRESERVES a well-formed user-prompt `PromptContext` end-to-end (cosmos-context-chip-historical-not-showing-v2). THE BUG: `validateConversationTurn` rebuilt a `user-prompt` as `{kind,id,ts,text}` and DROPPED `raw.context`; BOTH `conversation:fetch` (mount) and `conversation:update` (completed run) run every turn through it, so the historical (transcript-sourced) turn ALWAYS reached the renderer with `context` stripped → `PromptContextChip` got `undefined` → the chip vanished the instant the live entry (which is ref-seeded and never crosses this validator) was replaced by the validated transcript turn. THE FIX: split the combined `user-prompt`/`assistant-text` case so `user-prompt` carries through `{...,...(ctx?{context:ctx}:{})}` where `ctx` is the boundary-validated context; a malformed/absent context drops to no-context (turn still valid, never crashes — FR-118). ONE SOURCE: the marker parser's internal schema is EXPORTED as `validatePromptContext(parsed): PromptContext|null` from `promptContextMarker.ts` (browser+node safe, no fs/electron) and reused by BOTH the marker parser AND this boundary, so the two schemas can never diverge; nothing beyond the PromptContext whitelist (panel/tab/dock view labels) crosses. The test asserts `validateConversationTurn`/`validateConversationResult` preserve a context built via the REAL `serializePromptContextMarker`→`parsePromptContextMarker`→`validatePromptContext` round-trip (jira dock), drop a malformed context to no-context (turn kept), never attach context to `assistant-text`; plus focused `validatePromptContext` cases (panel-only / panel+tab / panel+tab+dock accepted; bad panel id / empty label / partial tab / dock-with-no-field rejected; junk never throws). RED→GREEN: the 3 carry-through cases fail when the validator drops `context`, pass with the split. | node-unit | `src/shared/ipc/conversation.validate.test.ts` |
| COSMOS-LIVE-BUBBLE-01 | After a Cosmos composer submit, the user's OWN prompt bubble is VISIBLE while generating, and it appears BEFORE the `TypingIndicator` ("…") in DOM order, with the captured-context chip ABOVE it (chip → bubble → dots) — the user must see their utterance registered, not just a spinner (cosmos-live-bubble-missing-while-generating-v1; chip-above ordering per cosmos-context-chip-position-and-historical-v1 #2). The bubble must SURVIVE the `agent:status 'started'` re-seed (which re-reads `lastPromptRef`) and must show the CLEAN utterance, never a raw `<cosmos:context>` marker (FR-024/FR-025). WHY A JSDOM ROW IS REQUIRED: the `live-generating` render is trivially correct in isolation and `reconcileTimeline` is node-tested, so neither catches a regression where the seed DROPS `promptText` (an early `conversation:onUpdate` or a `'started'` status replacing `live` without the text). The jsdom test renders the REAL `CosmosPanel` (wrapped in `ActiveComposerProvider` + a `TooltipProvider`, with a `window.cosmos` stub), drives the published `'cosmos'` composer `onSubmit` (the Enter path) AND the `agent:status 'started'` callback, and asserts the bubble text is present + precedes the dots through the WHOLE onSubmit→setLive→reconcile→render chain (RED when either seed omits `promptText`, GREEN with it). VERDICT for the reported symptom: the on-disk seed/render path is CORRECT (test green), so the user's "no bubble" runtime was STALE DEV — an un-restarted `npm run dev` (the Cosmos context feature needs a full restart for the new preload surface); the test stays as the regression guard. | jsdom + node-unit | `src/renderer/cosmos/CosmosLiveBubble.dom.test.tsx`, `src/renderer/cosmos/cosmosConversation.test.ts` (reconcile) |

| COSMOS-STREAM-PROGRESS-01 | The Cosmos timeline STREAMS the agent's in-flight steps instead of showing a lone spinner until completion (cosmos-agent-progress-not-streaming-v1). ROOT CAUSE: the headless `claude -p --output-format json` run surfaces only `started`/`completed`/`error` status (stdout merely accumulated), and `conversation:update` was pushed ONLY on `completed` (`index.ts` `pushConversationUpdateToRenderer`), so the renderer saw `started`→`TypingIndicator`→`completed`→one full-transcript dump. THE FIX (contained, no new IPC): `claude` APPENDS to the default-session transcript jsonl incrementally during a run, so a main-side `TranscriptWatcher` POLLS that transcript while a run is in flight and pushes an incremental `conversation:update` on each real change — reusing `TranscriptReader`→`validateConversationResult`→`ConversationChannel.Update`. Armed on `agent:status 'started'`, stopped on 'completed'/'error' AND on teardown (reload/close/dispose) — never leaks a timer (mirrors `fsExplorer.stopAll()`); polls (not `fs.watch`) because the first run's transcript file/dir does not exist at arm time; coalesces a burst via a cheap change signature (an unchanged poll never re-pushes). RENDERER: `CosmosPanel.onUpdate` NO LONGER calls `setLive(null)` (that killed the spinner on every incremental push) — ONLY `agent:status 'completed'`/'error' clears `live`; `reconcileTimeline` keeps each turn EXACTLY ONCE across the live↔transcript overlap (the provisional live prompt bubble + context chip is suppressed once the transcript carries the run's user-prompt → no double bubble/chip; a LIVE surface stays authoritative over its transcript copy so a still-pending interactive surface is not replaced by a display-only no-op mid-run). DUP-CHIP REGRESSION (cosmos-streaming-duplicate-context-chip-v1): the provisional-prompt suppression must NOT be gated on exact `live.promptText` equality — that missed a CROSS-PANEL submit (Jira/Slack/etc), whose live seed has `promptText` UNDEFINED (`CosmosPanel.lastPromptRef` is cosmos-only) but a `promptContext` SET, so the live chip duplicated the transcript user-prompt turn's marker-parsed chip mid-stream (dropping to one only on completion when `live`→null). ROBUST signal: suppress when the transcript's LAST turn is a `user-prompt` (a run's prompt just landed, response pending — a COMPLETED prior run always ends in its assistant/surface response, never a bare trailing user-prompt) OR the exact-text match (keeps a cosmos run suppressed after its assistant/tool turns stream in); pre-catch-up (transcript ends in the prior response) still shows the provisional instantly on Enter (FR-024). EMPTY-BUBBLE REGRESSION (cosmos-streaming-duplicate-context-chip-v1, baseline fix): the `last.kind`/exact-text guards still missed the WHOLE-STREAM cross-panel case — once the run's ASSISTANT/TOOL turns stream in, the transcript's last turn is NO LONGER the user-prompt and `promptText` is undefined, so the `live-generating` provisional re-appeared carrying `promptContext` but no `promptText` → an EMPTY user message showing only the context chip. ROBUST primary signal: a renderer-internal `LiveInFlight.baseline` (the transcript turn COUNT captured by `CosmosPanel` at run start, from a `readRef` mirror, on BOTH the `onSubmit` optimistic seed AND the `agent:status 'started'` seed, re-captured per run; carried through the `surface` variant too) — `reconcileTimeline` suppresses the provisional ENTIRELY when `turns.length > live.baseline` (the transcript grew past where the run began → it already carries the run's prompt + streamed turns). No IPC change. node-unit asserts: a `generating` live with `promptContext`, UNDEFINED promptText, `baseline:N`, on a transcript of length > N whose LAST turn is assistant-text/tool-call (NOT user-prompt) → `live-generating` carries NO promptContext/promptText (spinner only, no empty bubble) — RED pre-baseline-fix (the `last.kind` signal fails there); and `length === baseline` (not yet grown) still shows the provisional. Earlier cases: a `generating` live with `promptContext` and (a) matching promptText AND (b) UNDEFINED promptText, on a transcript ENDING in the run's user-prompt → spinner only → one chip; a pre-catch-up case (transcript ends in a prior assistant turn) still carries promptText+promptContext. node-integration drives the watcher with an injected scheduler + a growing-transcript reader: each growth → ONE push of the accumulated turns, an unchanged/burst poll coalesces, `stop()` clears the interval (no leak), `start()` re-baselines so a new run only streams its own turns (RED pre-fix: no watcher exists). jsdom renders the REAL `CosmosPanel`, submits, fires `'started'`, then an incremental `conversation:update` and asserts the streamed assistant/tool turns appear WITH the spinner still present and the prompt rendered once, and that ONLY `'completed'` clears the spinner (RED pre-fix: `onUpdate`'s `setLive(null)` kills the spinner immediately, confirmed). | node-integration + jsdom + node-unit | `src/main/fs/transcriptWatcher.integration.test.ts`, `src/renderer/cosmos/CosmosStreamingProgress.dom.test.tsx`, `src/renderer/cosmos/cosmosConversation.test.ts` (reconcile dedup) |
| PANEL-TABS-TREE-01 | The pure cross-panel tab-tree derivations (cosmos-panel-tab-list-v1). `toPanelTabGroups(registry, order, labels)` shapes the live `PanelTabsRegistry` into ordered groups: a panel ABSENT/`null` in the registry is OMITTED (FR-006), a published panel with ZERO tabs yields an EMPTY group (FR-020), groups follow the `order` arg, and a malformed entry (tabs not an array) / malformed tab (missing id|label) is WARNED + SKIPPED never crashed (FR-022); a missing registry → no groups. `reconcileSelectedContext(selected, groups)` keeps a tree-click selection honest (FR-017): no selection / tab-less selection unchanged; a renamed source tab RELABELS the tab segment; a CLOSED tab or a GONE panel CLEARS the selection (null); an unchanged selection returns the SAME reference (so a caller's effect/setState is a no-op). Pure node module. | node-unit | `src/renderer/panelTabs/panelTabsTree.test.ts` |
| PANEL-TABS-CHIP-01 | The pure selection→chip mapper (cosmos-panel-tab-list-v1). `panelTabChipFor(selected)` returns the composer's `kind:'panel-tab'` ContextChipData (`{kind, panel:{id,label}, tab:{id,label}}`) for a panel+tab selection — carrying ONLY non-secret panel/tab labels (FR-011), NO dock dimension (FR-018) — and `undefined` for no selection or a panel-only (tab-less) selection. Supports a `terminal` panel id (T1). | node-unit | `src/renderer/cosmos/cosmosSelectedContext.test.ts` |
| PANEL-TABS-PROVIDER-01 | The App-root live cross-panel tab-list READ seam (cosmos-panel-tab-list-v1, the crux — sibling to `ActiveComposerProvider` routing + the lossy debounced `SessionRegistry`). A panel's `usePublishPanelTabs(panelId, memoizedTabs)` reaches a `useAllPanelTabs()` subscriber as a group (FR-008/FR-009); a tab-state change RE-PUBLISHES (reactive, version-counter, not stale); two panels publish independently and a panel CLEARS from the registry when its publisher unmounts (the effect cleanup publishes `null`, FR-006). Ref-backed registry + version counter (mirrors `ActiveComposerProvider`). | jsdom | `src/renderer/panelTabs/PanelTabsProvider.dom.test.tsx` |
| PANEL-TABS-TREE-UI-01 | The `PanelTabTree` right-column tree (cosmos-panel-tab-list-v1, design §2). Renders one group per panel with its tab rows (FR-005/FR-007); a published panel with zero tabs shows a quiet "No open tabs" line (FR-020); no in-scope panels → one calm centered "No open tabs in other panels" block (FR-021). Reuses the FileTree `role="tree"` roving keymap (FR-003): a click activates a tab row (no navigation, FR-012), and ArrowDown→Enter activates via keyboard; clicking a group header collapses it (its tab rows disappear). PER USER REQUEST the tree is a pure PICKER — the selected tab reflects in `aria-selected` for a11y only, with NO visual highlight (no context-selected bar/`data-context-selected`) and NO active-source dot (both removed from the design's D-15 — flagged for designer reconciliation). PINNED-STATE MARK (cosmos-home-favorite-tabs-v1, D-15): an ALREADY-PINNED source row (stub `isPinned`→true for one tab) carries `text-primary` on its leading icon (NOT `text-muted-foreground`) + a `font-medium` (bold) label, driven by the SAME `isPinned` signal the Pin/Unpin menu uses; a non-pinned sibling keeps its muted icon + normal weight. | jsdom | `src/renderer/cosmos/PanelTabTree.dom.test.tsx` |
| COSMOS-PANEL-TABS-01 | The END-TO-END Cosmos panel-tab-list flow (cosmos-panel-tab-list-v1). Renders the REAL `CosmosPanel` under the real `PanelTabsProvider`+`ActiveComposerProvider`+`SessionProvider` (jira ENABLED so its group is visible — the tree filters by rail visibility, FR-006), a sibling Jira `usePublishPanelTabs` publisher, and a probe rendering the REAL composer `ContextChip` from the published 'cosmos' `ComposerConfig`. A tree row click → the composer chip shows the `Jira › Sprint board` panel+tab breadcrumb (FR-014); calling the published `onSubmit` → `agent.submit` gets a payload whose `target` stays `'generated-ui'` and whose `<cosmos:context>` marker names that panel+tab (FR-015), then the COMPOSER chip clears (one-shot, OQ-2) while the timeline's live in-flight `PromptContextChip` (a separate `role="note"`) shows the submitted context; re-selecting REPLACES (FR-016); a CLOSED selected tab clears the chip (FR-017); `onSubmit(..,{contextDismiss:'all'})` drops the jira selection so the marker names `cosmos` not `jira` (FR-016). Chip assertions are scoped to a `composer-chip-slot` test-id to disambiguate the composer chip from the timeline chip. | jsdom | `src/renderer/cosmos/CosmosPanelTabList.dom.test.tsx` |
| PROMPT-CTX-MARKER-TERMINAL-01 | The `<cosmos:context>` marker codec round-trips a TERMINAL panel+tab selection (cosmos-panel-tab-list-v1 T1): `PANEL_IDS` + `DOCK_KIND_BY_PANEL` accept `'terminal'` (`DOCK_KIND_BY_PANEL.terminal = null` — terminal has no dock), and `serialize→parse` preserves `{panel:{id:'terminal',label},tab:{id,label}}`. Co-extends PROMPT-CTX-MARKER-01 (the four existing panel ids are unchanged). Likewise `validatePromptContext` (the shared marker/IPC-boundary schema) accepts a terminal panel+tab context (extends CONV-VALIDATE-CTX-01). | node-unit | `src/shared/promptContext/promptContextMarker.test.ts`, `src/shared/ipc/conversation.validate.test.ts` |

| TERM-RESTART-RESUME-01 | A genuinely-dead terminal `claude` recovers by RESUMING the SAME session id, never minting fresh (terminal-session-unnecessary-restart-v1, ARCHITECTURE.md §4.1 continue-don't-restart). ROOT CAUSE: cosmos does NOT respawn on lock/sleep/wake — the upstream `claude` process exits when its API/stream connection drops on a lock, falling through `PtyManager`'s `onExit` (NOT disposed, FAR past the 4000ms `RESUME_FAILURE_WINDOW_MS`, NOT an "already in use" rejection) to the plain exit → the renderer's exit banner. The CORE fix: the exit-banner `pty:restart` (`isExplicitRestart=true` threaded through `paneSpawnFor`→`resolvePaneSpawn`) re-`--resume`s the recorded id in the recorded cwd to restore the transcript/context — it previously create-or-continued via `--session-id`, which did NOT arm the resume-failure / in-use backoff. A benign cwd-less `pty:start` re-issue (StrictMode double-start) STILL keeps `--session-id` (its recorded session may be an empty just-minted-then-killed id `--resume` would reject "No conversation found"), so only the explicit restart resumes (KNOWN TENSION below). The node-integration harness reproduces the index.ts `pty:start`/`pty:restart` handler wiring against the REAL PtyManager (injected node-pty spawn + injected clock to age past the window) + real `resolvePaneSpawn` + real `planResumeRetry`: (1) CORE — a long-lived abnormal exit surfaces a plain exit then `pty:restart` spawns `--resume <sameId>` in the recorded cwd, id UNCHANGED, no mint (RED before the fix: `--session-id`; GREEN after, confirmed); (2) `pty:restart` with NO recorded id falls back to a fresh `--session-id`; (3) NO-RESPAWN INVARIANT — a `powerMonitor 'suspend'` (log-only handler) and a renderer active-toggle (resize-only) leave a live PTY running (no kill, no second spawn, no exit); (4) ORPHAN RECOVERY preserved — an in-use rejection on the restarted id routes to `onSessionInUse` (suppressing the plain exit) and the `planResumeRetry` backoff re-spawns the SAME `--resume <id>`, never a fresh mint. The pure resolver branch selection is co-covered node-side. | node-integration + node-unit | `src/main/pty/sessionRestart.integration.test.ts`, `src/main/pty/paneSpawn.test.ts` |
| TERM-EXIT-COPY-01 | The terminal exit-banner copy is honest about restart recovery (terminal-session-unnecessary-restart-v1). `formatExit` summarizes the exit (a spawn/PATH `error` verbatim; else `claude exited (exit code N[, signal M])`). `exitRecoveryHint` returns the recovery line ONLY for a genuine live-session death (an exit code/signal) — it promises that Restart RESUMES the conversation (transcript preserved) AND is honest that auto-accept mode (shift+tab) must be re-enabled because it is process-local TUI state that cannot survive the death — and returns null (no hint) for a spawn/PATH `error` (no transcript to resume). The copy never claims auto-accept mode was restored. Pure copy in `terminalExit.ts` (no xterm/React import), so it is node-tested in isolation; `TerminalPanel.tsx` does the trivial conditional render of the hint beneath the message. | node-unit | `src/renderer/terminal/terminalExit.test.ts` |
| CAL-HIDE-OVERLAP-01 | Hiding a calendar in the legend removes EVERY one of its week/day schedule events, INCLUDING overlap-lane-split ones (calendar-hidden-overlapping-event-remains-v1). The report blamed the overlap render path; investigation DISPROVED both an id-space mismatch and an overlap/React-key bug — the native aggregated default view (`listAggregatedEvents`) stamps every event's `calendarId` with its owning calendar id (`googleCalendarManager.ts:320`) AND the legend uses the SAME id (`googleCalendarSurfaceBuilder.ts:83-90` legend `id:calendar.id`), both drawn from the one `ordered` list, so `event.calendarId ⊆ legend ids` is GUARANTEED and `visibleEvents` (`logic.ts:272-281`) always matches. These END-TO-END render rows (the path that had NO covering test — pure `logic.test`/`scheduleLayout.test` cover the filter + lane math only) render the REAL EventList→ScheduleView→DayColumn→EventBlock + the REAL legend `role="switch"` toggle and assert the hidden calendar's blocks leave the DOM across THREE overlap shapes: (a) two cal-a events overlapping EACH OTHER, (b) a cal-a event overlapping a still-visible cal-b event (shared lane cluster reflows), (c) a SHARED meeting carrying the SAME Google event id on two calendars (perfect overlap ⇒ duplicate React key) where hiding one calendar correctly leaves only the OTHER copy. A 4th harness (not committed) confirmed the panel's `CalendarVisibilityContext` path behaves identically to the local fallback. All GREEN; the reported failure does not reproduce with legend-matched data. NOTE — the only shape that lingers is data whose `calendarId` is absent/outside the legend (spec defines an un-tagged event as ALWAYS visible, `logic.ts:269-270`); the native path cannot produce it, and cross-calendar dedup of a shared meeting is a product/design decision (architect), not a filter defect. | jsdom (real render path) | `src/renderer/calendar/googleCalendarCatalog/CalendarHiddenOverlap.dom.test.tsx` |

| HOME-FAVORITES-01 | The PURE Home-favorites ops (cosmos-home-favorite-tabs-v1). `cosmosTabs`: `favoriteId` is `fav:${panelId}:${tabId}` (deterministic); `appendFavorite({source,label})` appends a closeable favorite AFTER the pinned default WITHOUT activating it (pinning is NON-DISRUPTIVE — `activeTabId` is UNCHANGED, the user stays on the current tab; FR-010 user-feedback correction), and is IDEMPOTENT/de-duped by source (a repeat pin returns the SAME state reference, no duplicate); `isPinned` reflects pinned state; `closeCosmosTab` unpins a favorite + returns focus to the default. `homeFavorites`: `findLiveTab` reads the live source tab WITH its current `surface` (the live-mirror read), returns null (never throws) for a missing/null-surface/malformed registry; `reconcileFavorites` RELABELS a favorite on source-tab rename, KEEPS one whose source is gone (never auto-dropped, FR-031), and returns the SAME reference when unchanged; `validateFavorites` (the SINGLE shared validator in `shared/ipc/session.ts`, reused by the main `validateSnapshot` boundary) keeps only `{panelId,tabId,label}` for a known gateable panel + rebuilds to that whitelist (drops a secret-shaped extra key, terminal/unknown panel, empty/ non-string fields, de-dupes), treats undefined/non-array as empty; `toHomeFavorites`↔`favoritesToTabs` round-trip preserves source ids + label in order. Main `validateSnapshot` carries a valid `favorites` through additively with NO schema bump (still 8), drops malformed/secret/terminal/unknown entries, and OMITS the field when empty/pre-feature. | node-unit | `src/renderer/cosmos/cosmosTabs.test.ts`, `src/renderer/cosmos/homeFavorites.test.ts`, `src/main/session/sessionSnapshot.test.ts` |
| SESSION-FAVORITES-EAGER-01 | Home favorites persist EAGERLY, not on the shared trailing debounce (bug favorites-lost-on-restart-v1). ROOT CAUSE: `SessionRegistry.setFavorites` used `schedule()` — the single `SAVE_DEBOUNCE_MS` (600ms) timer shared by every contribution, reset on each `report`/`setX`, so an actively-changing session could perpetually defer the favorites save; and a dev HMR / Vite reload inside that window pre-empted it (a PARTIAL HMR update fires NEITHER `pagehide` NOR `beforeunload`, so the `SessionProvider` teardown `flush()` never ran) → the just-pinned favorite never reached disk, and the next full reload re-seeded `CosmosPanel` from the stale app-start snapshot (`useLoadSession` loads once) → favorite vanished "on every restart". THE FIX: `setFavorites` records the list then calls a shared private `saveNow()` (cancel the pending timer + save immediately — the same body `flush()` now delegates to), so a pin/unpin reaches disk on the spot; a full reload/relaunch then restores it. OTHER non-panel contributions (`setOpenPromptPosition`/`setEnabled`/panel `report`) KEEP the trailing debounce (no regression — `openPromptPosition` intentionally debounces a drag storm). Node-unit asserts: `setFavorites` calls `save` IMMEDIATELY (WITHOUT `sched.run()`) carrying the favorite (RED pre-fix — was scheduled); a `report` STORM that perpetually resets the shared timer still lands the favorite eagerly (and carries the other pending contributions, current-state-early, safely); and `setOpenPromptPosition` STILL does not save until the scheduler fires (no eager regression). INADEQUATE (see SESSION-FAVORITES-RESTART-02): this row is a SessionRegistry NODE-UNIT with a `vi.fn()` `save` spy — it proved favorites reach the spy eagerly but NEVER exercised the real save→validate→load→seed→re-bind round-trip, so it could not catch the round-2 regression (the eager save WIPING the favorite's source panel on a fresh-mount registry). KEPT as the eager-debounce contract guard; the round-trip guard is the new row. | node-unit | `src/renderer/session/sessionRegistry.test.ts` |
| SESSION-FAVORITES-RESTART-02 | Home favorites survive a RESTART round-trip — the source panel is NOT wiped, and a dev Fast-Refresh remount does not drop the favorite (bug favorites-lost-on-restart-v2; the v1 eager fix was necessary but INSUFFICIENT). ROOT CAUSE (runtime/ordering, not wiring): on a fresh relaunch the `SessionRegistry` starts with EMPTY contributions, and `CosmosPanel` (mounted BEFORE the generative panels in the rail — App.tsx) fires an EAGER favorites save during its mount effect, BEFORE the Jira/Slack/Confluence/Calendar panels re-report. `assembleSnapshot` fills every un-reported panel with an EMPTY default, so the eager save persists `{ favorites, EMPTY panels }` — WIPING the favorite's SOURCE panel from disk (NOT the favorite reference itself; the failure is PRESENT-but-gone-source, save-side). The 600ms debounced panel reports heal disk only if the app survives the window; a dev HMR / quick relaunch inside it makes it stick → the favorite re-binds to nothing (the calm "no longer open" state). The "absent, as if never pinned" symptom is a SECOND path: a dev Fast-Refresh REMOUNT of Home re-runs the `useState` initializer against the STALE app-start snapshot (the registry instance survives) and the eager favorites effect then persists `[]`, wiping the favorite. THE FIX: (1) `SessionRegistry.seed(snapshot)` populates the contributions from the restored snapshot at mount (SessionProvider, mirroring the pre-existing per-mount `enabled` seed) so any early/eager save preserves the restored panels regardless of panel-mount order or debounce timing — terminal EXCLUDED (main re-enriches/drops it from its live PTY session map; it also reports before Cosmos); (2) `CosmosPanel` seeds its initial favorites from the LIVE `registry.getFavorites()` first (falling back to the restored snapshot) so a Fast-Refresh remount re-seeds the truly-pinned set instead of resetting to none — a genuine unpin leaves the registry favorites `[]`, respected. JSDOM round-trip renders the REAL `CosmosPanel` under the REAL `SessionProvider`/`SessionRegistry` wired to a disk-equivalent store (JSON serialize/parse + the SHARED `validateFavorites` the main `validateSnapshot` delegates to) with a faithful `JiraSource` (hydrates from the restored slice, publishes live, reports to the session registry): PIN → save → RESTART (reload + re-mount) asserts the source panel STILL on disk + a second restart re-binds the favorite to a POPULATED source (RED pre-fix: `panels.jira.tabs` `[]` after restart, favorite goes gone-source); and a keyed Fast-Refresh REMOUNT (SessionProvider stays mounted, snapshot prop stale) keeps the favorite in the strip AND on disk (RED pre-fix: favorites wiped). NODE-UNIT (`sessionRegistry.test.ts`) locks the `seed`/`getFavorites` contract: a seeded fresh registry's eager favorites save preserves the source panel (vs. un-seeded → empty panel — the bug), `getFavorites` tracks the live contribution (seeded set, then `[]` after a genuine unpin), and `seed` triggers no save. MANUAL (`npm run dev`, NOT exercised here): pin a favorite → restart → persists + renders the live source. | node-unit + jsdom | `src/renderer/session/sessionRegistry.test.ts`, `src/renderer/cosmos/CosmosFavoriteRestartRoundTrip.dom.test.tsx` |
| COSMOS-FAVORITE-TABS-01 | The END-TO-END Home favorite-tab flow (cosmos-home-favorite-tabs-v1). Renders the REAL `CosmosPanel` under the real `PanelTabsProvider`+`ActiveComposerProvider`+`SessionProvider` (jira enabled) with a sibling publisher (the `usePublishPanelTabs` path — now carrying each tab's live `surface`); `ActiveTabSurface` is STUBBED to print its surface's `surfaceId` so the inline live-mirror is assertable without the A2UI SDK/catalog. RIGHT-CLICK a tree TAB row → "Pin" → a favorite appears in Home's strip AFTER the pinned default (with its source panel glyph + the source label) but pinning is NON-DISRUPTIVE — the default tab STAYS active + the tree stays shown (no navigate-on-pin; FR-010 user-feedback correction); CLICKING the favorite renders the SOURCE tab's live surface inline (the default conversation placeholder disappears); a source RE-PUBLISH of a new surface updates the inline mirror (proves LIVE, not a snapshot); a GONE source shows the calm "This tab is no longer open" state + an Unpin button and is NOT auto-dropped; the favorite's close `X` AND the context-menu Unpin both unpin + return to the default; a pinned row's menu reads "Unpin"; a TERMINAL tree row IS pinnable now (cosmos-terminal-favorite-multiplex-v1 relaxed FR-040 — its "Pin" is ENABLED, no "can't be pinned" reason, and pinning appends a terminal favorite after the default, non-disruptively — see TERM-FAVORITE-MULTIPLEX-01); restored `favorites` in the session snapshot seed the strip on mount (FR-030). FULL-WIDTH SOURCE MIRROR (user-feedback corrections): a FAVORITE tab HIDES the cross-panel tree (the timeline|tree split + the tree render ONLY on the default tab — `queryByRole('tree')` is present on default, absent on a favorite, restored on switch-back) AND HIDES the docked Cosmos composer (Home publishes a NULL `'cosmos'` config while a favorite is active — a probe reading `useActiveComposerConfig('cosmos')` is non-null on default, null on a favorite), and renders the SOURCE panel's OWN floating Open Prompt whose `onSubmit` is the source's published config (asserted by stubbing `PromptComposer` to capture props: on a favorite the rendered composer's `ariaLabel` is the Jira config's and clicking it calls the JIRA `onSubmit` spy — i.e. routes to the SOURCE target, not the Cosmos conversation). The PanelTabTree menu primitive wiring (unpinned→Pin fires onPin, pinned→Unpin fires onUnpin, and — post cosmos-terminal-favorite-multiplex-v1 — a TERMINAL row's Pin is ENABLED and fires onPin) is also asserted in isolation. NOTE: Radix menus portal to `document.body` — the test relies on RTL auto-cleanup (NOT a manual `document.body.innerHTML=''`, which races portal removal) and stubs `scrollIntoView`/pointer-capture (jsdom-missing); `ActiveTabSurface` + `PromptComposer` are stubbed so the inline mirror + the favorite Open-Prompt wiring are assertable without the A2UI SDK / the composer's measure-gate. | jsdom | `src/renderer/cosmos/CosmosFavoriteTabs.dom.test.tsx`, `src/renderer/cosmos/PanelTabTree.dom.test.tsx` |
| COSMOS-HOME-FOOTER-01 | The Cosmos docked "Home" name+status FOOTER is surface CHROME, DECOUPLED from the composer config (home-favorite-missing-footer-v1) — it renders on the cosmos/docked surface REGARDLESS of whether a `'cosmos'` composer config is published, and the composer→footer order is preserved when one is. THE BUG: a Home FAVORITE tab lost the footer entirely. ROOT CAUSE: `SharedComposer` had a blanket `if (!config) return null` BEFORE the docked branch that renders `<PanelFooter surfaceName="Home"/>` below the composer band (footer-placement-cosmos-terminal-v1); a favorite tab publishes a NULL `'cosmos'` config (CosmosPanel hides the docked Cosmos composer + overlays the SOURCE panel's floating Open-Prompt — see COSMOS-FAVORITE-TABS-01's full-width mirror), so `config` was null → the early return dropped composer AND footer → no footer. THE FIX: drop the blanket early-out; build the composer element only when `config` is present; the docked branch renders the footer ALWAYS (idle "Home" when config is null via `config?.busy`, busy-driven otherwise) and the composer BAND only when present; the floating branch keeps its null-config short-circuit (a disconnected integration shows no chrome). Extends footer-placement-cosmos-terminal-v1 (same file) — does NOT contradict the composer→footer order invariant, re-asserted in the config-present row. JSDOM (real `SharedComposer surface="cosmos"`): NO published cosmos config (favorite-active) → the "Home" `PanelFooter` (aria-label "Panel" / text "Home") IS present AND the composer textarea is ABSENT (RED before the fix — empty `<div/>`, no footer); config present → BOTH present, composer precedes footer in DOM (order kept) + the existing `pb-8` gap row stays green. | jsdom | `src/renderer/app/CosmosFooterOrder.dom.test.tsx` |
| CF-FAVORITE-WAITING-01 | A Confluence Home favorite mirrors its source tab IFF the source tab carries a `TabSurface` — and the WAITING placeholder is CORRECT for a genuinely-null surface (confluence-favorite-waiting-v1). THE BUG: a pinned Confluence tab shows "Waiting for this tab's view…" forever. THE INVESTIGATION (this test is the proof): the publish path (`useGenerativePanelTabs` publish memo → `PanelTabsProvider` registry, identical for all four generative panels) and `FavoriteSurface` are NOT broken. Test A drives the REAL `useGenerativePanelTabs` for `target:'confluence'` end-to-end: a composed `ui:render` confluence frame lands in `t.surface`, publishes through the registry, and the favorite renders POPULATED (the stubbed `ActiveTabSurface` prints the live `surfaceId`) — GREEN with NO code change, disproving a Confluence-specific publish/memo defect. Test B publishes a confluence tab whose `surface` is null (the native-browsing state) and asserts the favorite shows WAITING. ROOT CAUSE (upstream, NOT the seam): Confluence renders its native browsing views (the `defaultFeed`/`searchContent` `ContentList` and the page-detail dock's native `PageDetail`, `genUiPage` in `ConfluencePanel.tsx`) as NATIVE React and NEVER writes a `TabSurface` into the tab record, so `t.surface` stays null and the published `LivePanelTab.surface` is null. Jira never hits this because its default view IS a pushed bound surface (`jira:requestDefaultView`); Slack shares the same gap for its native channel/history views (only composed surfaces mirror). The source fix — giving Confluence a published surface for its native views — is net-new feature/contract scope (architect). CLOSED by cosmos-native-view-mirror-surface-v1 (see NATIVE-MIRROR-01): the SAME test file now ALSO asserts the GREEN feature (a native-view `mirrorSurface` → POPULATED). | jsdom | `src/renderer/cosmos/ConfluenceFavoriteWaiting.dom.test.tsx` |

| NATIVE-MIRROR-01 | A Home favorite mirrors a Confluence/Slack tab's NATIVE browsing view, not only an agent-composed surface (cosmos-native-view-mirror-surface-v1; closes CF-FAVORITE-WAITING-01). THE GAP: native-first panels render native React and never write `tab.surface`, so a favorite of a natively-browsing tab showed WAITING forever. THE FIX: `LivePanelTab`/`GenerativeTab` gain an additive favorite-only `mirrorSurface?: TabSurface\|null`; the native panels lift their on-screen native data and build a SECRET-FREE bound surface via the RELOCATED shared `surfaceBuilders/{confluence,slack}SurfaceBuilder` (`nativeMirror.buildConfluenceMirror`/`buildSlackMirror` select-the-view + wrap to a display-only `TabSurface`, fresh requestId per build); `FavoriteSurface` resolves `mirrorSurface ?? surface`. The publish projection (`livePanelProjection.projectLivePanelTab`) publishes them MUTUALLY EXCLUSIVELY (`mirrorSurface: surface ? null : (mirrorSurface ?? null)`) so the favorite always shows exactly what the source shows. Built only while the source tab is PINNED — gated by a reverse "pinned sources" channel on `PanelTabsProvider` (`publishPins`/`usePinnedSources`). Renderer-only, non-secret, NEVER persisted (`buildGenerativeTab` does not whitelist it). NODE-UNIT: `nativeMirror.test.ts` (each view kind → right surfaceId + seeded rows; null→null; fresh requestId; Slack rows carry resolved `userName` — OQ-5 reuses the renderer's already-resolved native data; no token); `livePanelProjection.test.ts` (composed present → mirror nulled; native present+no surface → carried; both absent → both null). RELOCATED builders covered at the shared module (`src/shared/surfaceBuilders/{confluence,slack}SurfaceBuilder.test.ts` — moved from `src/main`, builder output byte-identical; the main `*Adapter.test.ts` re-export callers stay green). JSDOM (extends CF-FAVORITE-WAITING-01's file): a confluence tab with a native FEED `mirrorSurface` → POPULATED (prints `confluence-feed`, RED before the feature — surface null could only WAIT); a Slack analog (`slack-channels`) → POPULATED; a composed-surface present (mirror nulled by the projection) → shows the composed surface; the reverse pinned-sources channel flips a `usePinnedSources()` read from false→true on `publishPins` (the gate). MANUAL (`npm run dev`, NOT exercised here): pin a Confluence page + a Slack channel → favorite shows the live native view. | node-unit + jsdom | `src/renderer/cosmos/nativeMirror.test.ts`, `src/renderer/cosmos/livePanelProjection.test.ts`, `src/shared/surfaceBuilders/confluenceSurfaceBuilder.test.ts`, `src/shared/surfaceBuilders/slackSurfaceBuilder.test.ts`, `src/renderer/cosmos/ConfluenceFavoriteWaiting.dom.test.tsx` |

| CAL-FAVORITE-WAITING-01 | A Google Calendar Home favorite mirrors its source tab's month/week view instead of showing "Waiting for this tab's view…" forever (calendar-favorite-waiting-v1). DISTINCT from CF-FAVORITE-WAITING-01/NATIVE-MIRROR-01: the calendar is NOT a "native React, never writes `tab.surface`" gap — its pushed `target:'google-calendar'` default-view frame IS filed into `tab.surface` by the shared `useGenerativePanelTabs` hook (NO `onUnsolicitedFrame` interceptor routes it away), and the favorite resolves `mirrorSurface ?? surface` → it mirrors `tab.surface` whenever it exists. So NO `buildCalendarMirror`/`nativeMirror` branch is needed. ROOT CAUSE: `GoogleCalendarPanel`'s default-view FETCH was gated on `active`, so a pinned-but-HIDDEN calendar tab (e.g. after a restart — the live default view, `composed:false`, is NOT persisted by `buildGenerativeTab` and re-fetches on restore; all panels are `forceMount`ed so a hidden panel stays mounted with `active===false`) never fetched while the user sat in Home → `tab.surface` stayed null → the favorite waited forever. THE FIX: also fetch when the active tab is PINNED, reusing the existing reverse pinned-sources channel (`usePinnedSources`/`pinnedSourceKey` — the OQ-3 gate Confluence/Slack use): gate becomes `(active || isActivePinned) && isConnected && activeTab && !activeTab.surface && …`. A non-pinned hidden tab still does NOT eager-read (pre-fix policy preserved for the common case). No new builder, IPC, or contract — the calendar's own surface IS the mirror. JSDOM (sibling file): drives the REAL `GoogleCalendarPanel` (`active={false}`, connected, a RESTORED tab `g1` with NO surface) + a real `FavoriteSurface` for `g1` with `requestDefaultView` mocked to push a `google-calendar` frame and `ActiveTabSurface` stubbed to print `surfaceId`, scoped under a `home` container. Test A: pinning `google-calendar:g1` → the hidden tab fetches → favorite shows `google-calendar-default-view` (POPULATED), RED before the gate change (favorite shows WAITING, `requestDefaultView` never called); Test B: an UNPINNED hidden tab never fetches → favorite stays WAITING (no regression, green both ways). MANUAL (`npm run dev`, NOT in CI): connect Calendar → pin a tab → restart → go to Home → favorite shows the month/week view. | jsdom | `src/renderer/cosmos/CalendarFavoriteWaiting.dom.test.tsx` |
| TERM-FAVORITE-MULTIPLEX-01 | A terminal tab is pinnable as a Home favorite — a renderer-side xterm MIRROR of the same PTY (cosmos-terminal-favorite-multiplex-v1; relaxes cosmos-home-favorite-tabs-v1 FR-040). THE LOAD-BEARING GUARANTEE (SC-004/FR-005): a `mirror` (non-owning) `TerminalView` NEVER drives the shared PTY lifecycle — mounting + unmounting it calls NEITHER `pty.start` NOR `pty.dispose` NOR `pty.restart` (a naive 2nd mount would dispose the shared PTY on every Home tab switch and kill the source terminal); the OWNING view (no `mirror`, `autoStart`) is the regression guard (still `start`s on mount + `dispose`s on unmount). RED without the `!mirror` gates (confirmed: mirror unmount called dispose). The mirror is reused-in-place (the SAME `TerminalView` + a `mirror` prop) with xterm + the `../fileExplorer` (Monaco) barrel MOCKED — the behavior under test is the pty-lifecycle GATING in the mount/cleanup effect, not xterm rendering. FAVORITE BRANCH (FR-009/FR-013/FR-014): `FavoriteSurface` branches on `source.panelId==='terminal'` (before the `favoriteCatalogHosts` lookup) to `TerminalFavoriteSurface`, whose three states key off the live registry — GONE (source pane not published) → calm "no longer open" + Unpin; WAITING (pane published but no `serialize` ⇒ PTY not live) → calm waiting; POPULATED (`serialize` present) → the mirror seeded with `initialScrollback={serialize()}` (asserted via a STUBBED `TerminalView` exposing its `paneId`/`mirror`/`initialScrollback` props). The mirror is `React.lazy`-imported behind `Suspense` so the Monaco import stays OUT of the `FavoriteSurface`/`CosmosPanel` module graph (eager import crashed every favorites `.dom.test` — confirmed `queryCommandSupported`); a real-mirror render asserts via `await findBy…`. CosmosPanel pin-terminal: pinning a terminal tree row appends a terminal favorite after the default (non-disruptive — not activated, so the lazy mirror is not rendered). NODE-UNIT: widened `validateFavorites`/`FAVORITE_PANEL_IDS` KEEP a `{panelId:'terminal',…}` favorite (secret-free whitelist rebuild) while still dropping unknown panels; `cosmosTabs` `favoriteId`/`appendFavorite`/`isPinned` admit a terminal source; the PURE `shouldDriveResize` resize-guard (FR-011/FR-012) returns false for a null/zero-size container, true for a measurable one (RED→GREEN confirmed). | node-unit + jsdom | `src/renderer/terminal/terminalResize.test.ts`, `src/renderer/cosmos/cosmosTabs.test.ts`, `src/renderer/cosmos/homeFavorites.test.ts`, `src/main/session/sessionSnapshot.test.ts`, `src/renderer/terminal/TerminalViewMirror.dom.test.tsx`, `src/renderer/cosmos/TerminalFavoriteSurface.dom.test.tsx`, `src/renderer/cosmos/CosmosFavoriteTabs.dom.test.tsx`, `src/renderer/cosmos/PanelTabTree.dom.test.tsx` |

| COSMOS-KEYBOARD-TAB-NAV-01 | Home keyboard tab navigation (cosmos-home-keyboard-tab-nav-v1). The Home (`cosmos`) panel now participates in the SHARED global tab-cycle shortcuts via `useTabShortcuts`, gated on its `active` prop, cycling `cosmosTabs` order (default first, then favorites in pin order, wrap-around) through the pure `setActiveCosmosTab` — NO parallel nav helper (the hook owns the wrap/jump math, mirroring `cycleActiveId`). NODE-UNIT pins the cycle contract over `cosmosTabs`: `cycleActiveId` single-tab (default only) `tab:next`/`tab:prev` is a no-op (FR-009), forward `default→favJira→favSlack→`wrap-to-default + backward wrap (FR-001/FR-002/FR-003), and unpinning the ACTIVE favorite reconciles to the default (`closeCosmosTab`) then cycles the REMAINING tabs with no stale index (FR-010). JSDOM renders the REAL `CosmosPanel` with a stubbed `window.cosmos.shortcuts.onTrigger` (callback captured) + seeded favorites → firing `tab:next`/`tab:prev` moves the active strip tab with wrap, `tab:jump {index}` activates that tab (out-of-range = no-op, FR-014), `tab:last` activates the final tab, `tab:new`/`tab:close` cause NO membership change (Home OMITS both ops ⇒ structural no-op, Q4/Q5/FR-013), and with `active={false}` NONE of the commands change Home's active tab (FR-005). FR-008 (no stray char in a focused composer) is guaranteed by main-side `preventDefault` (§4.12) — main consumes the keystroke before the DOM sees it — so it is NOT re-tested in jsdom. The `TabShortcutOps` `onNewTab`/`onCloseTab` optionalization keeps the four generative panels + terminal (which pass both) unchanged — covered by the unchanged `useTabShortcuts.dom.test.tsx`. | node-unit + jsdom | `src/renderer/cosmos/cosmosTabs.test.ts`, `src/renderer/cosmos/CosmosKeyboardTabNav.dom.test.tsx` |

| TERM-EXPLORER-SHARE-01 | A terminal favorite mirrors the FULL Terminal view — the file-explorer split too, NOT only the shell (cosmos-terminal-favorite-explorer-share-v1; relaxes the multiplex base's "terminal pane only"). MECHANISM: "share the data instance, render two views" (Monaco's own model↔view split, applied to the viewer). The open-files SELECTION is lifted into an App-root paneId-keyed SHARED store (`OpenFilesProvider`, modeled on `PanelTabsProvider` — ref + `version`; `apply` runs the SAME pure `openFiles.ts` transitions), and each open TEXT file is backed by ONE shared `ITextModel` in a ref-counted registry keyed `cosmos-file://<paneId>/<relPath>`. The SOURCE mount stays the single fs owner (`fs:read` resolution via a new owning RESOLVER effect + `fs:watch` + persist/restore); the favorite mount (`{ mirror: true }`) is NON-OWNING. READ-ONLY content-sync (OQ-1 — no `fs:write`/edit/save). NODE-UNIT: `monacoModelRegistry.test.ts` (injected fake factory — same uri → one model; acquire/detach move the refcount; dispose ONLY when released AND refcount 0; detach-while-open / release-while-attached do NOT dispose; re-acquire clears the released latch; a different relPath is a different model; `syncText` setValue only when differing) + `openFiles.test.ts` kept GREEN unchanged (the lift moved only storage). JSDOM: `OpenFilesProvider.dom.test.tsx` (apply mutates the right pane + two same-paneId consumers both re-read; setLive/clear; pane-independence; a no-op transition does not churn); `useFileExplorerShare.dom.test.tsx` — (A) SINGLE-MOUNT REGRESSION (SC-007/OQ-4, the headline): go-live seed + ONE fs:read per restored path (resolver), StrictMode double-mount does NOT wipe the restored seed (`persist-open-files-restore-broken-v1` guard), `onOpenFilesChange` reports every change, open→resolve→ONE fs:read with NO re-read on re-click, adjacency-close, watch re-read + invalidate (siblings intact), teardown clears the shared store + releases models; (B) TWO-MOUNT CONTENT-SYNC (SC-002/003/004/005): one owning + one mirror hook for the SAME paneId — a mirror-initiated open appears in the owner + is resolved with exactly ONE owner fs:read (both read it), activate/close from either reflects in both, the mirror drives NO fs:watch/fs:read, a gone source (owner unmount) degrades the mirror to an empty entry; `MonacoTextModelRegistry.dom.test.tsx` (Monaco mocked): mounting a text file `acquire`s + `setModel`s its shared model, a file-switch `detach`es the old + `acquire`s the new, a same-file text change `syncText`s the shared buffer, unmount `detach`es (never disposes the shared model directly). The pre-existing `MonacoFocusNav.dom.test.tsx` monaco mock was enriched (setModel/getModel/createModel/Uri) so the new registry path is exercised without changing the focus wiring under test. MANUAL (`npm run dev`, NOT exercised here): pin a terminal with files open → the favorite shows the same open tabs + active file + content; open/close/activate from either view syncs; a disk change updates both. | node-unit + jsdom | `src/renderer/fileExplorer/monacoModelRegistry.test.ts`, `src/renderer/fileExplorer/openFiles.test.ts`, `src/renderer/fileExplorer/OpenFilesProvider.dom.test.tsx`, `src/renderer/fileExplorer/useFileExplorerShare.dom.test.tsx`, `src/renderer/fileExplorer/MonacoTextModelRegistry.dom.test.tsx` |

(Extend this table as scenarios are added. Mark unbuilt ones TODO so gaps are visible.)

## Known tensions (cross-scenario conflicts to respect)

- **SL-SCROLL-01 vs SL-SCROLL-02 (RESOLVED):** both ride `SLACK_LAYOUT_FILL_CLASS`. The
  resolution is: do NOT force `[&>*]:!flex-row` and do NOT add `flex-wrap`/`content-start`.
  The SDK keeps its natural direction — a Row interior splits lists side-by-side (SL-SCROLL-01);
  a Column interior stacks a header above its list (SL-SCROLL-02); per-list `flex-1 min-h-0
  overflow-y-auto` preserves independent scroll either way. A change to that class MUST keep
  BOTH visual tests green. See `feedback-slack-per-list-scroll` memory.
- **SL-SCROLL-03 vs SL-PAGE-01:** initial-bottom-jump must be mutually exclusive with
  near-top auto-load (the first non-empty render jumps to bottom; later grows anchor-preserve).
  A test for one must not assume the other fired.
- **TERM-RESTART-RESUME-01 (explicit restart `--resume`) vs the StrictMode idempotent re-start
  (`--session-id`):** both go through `resolvePaneSpawn`'s recorded-session branch, but they MUST
  emit DIFFERENT flags. An EXPLICIT exit-banner restart (`pty:restart`, `isExplicitRestart=true`) must
  `--resume <id>` (a genuinely-dead session has a transcript; resume restores it + arms the
  resume-failure/in-use backoff). A benign cwd-less `pty:start` re-issue (React StrictMode mount→
  dispose→remount, `isExplicitRestart=false`) must keep `--session-id` create-or-continue, because its
  recorded session may be an empty just-minted-then-killed id that `--resume` rejects with "No
  conversation found". A change to the recorded-session branch MUST keep BOTH the new explicit-restart
  `--resume` cases AND the existing StrictMode `--session-id` cases (`paneSpawn.test.ts`) green; the
  id is NEVER re-minted on either path.
